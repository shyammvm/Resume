name: Build and Publish Resume

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest
    outputs:
      pdf_path: ${{ steps.upload.outputs.artifact-name }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Compile LaTeX
        uses: dante-ev/latex-action@latest
        with:
          working_directory: resume
          root_file: resume.tex
          output_directory: ./build  # temporary folder

      - name: Upload PDF as artifact
        uses: actions/upload-artifact@v3
        id: upload
        with:
          name: resume-pdf
          path: build/resume.pdf

  deploy:
    runs-on: ubuntu-latest
    needs: compile  # waits for compile job
    steps:
      - name: Download PDF artifact
        uses: actions/download-artifact@v3
        with:
          name: resume-pdf
          path: public  # download to public folder

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public